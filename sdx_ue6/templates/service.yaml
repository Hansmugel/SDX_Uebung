apiVersion: v1
kind: Service
metadata:
  name: {{ include "sdx_ue6.fullname" . }}
  labels:
    {{- include "sdx_ue6.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    {{- include "sdx_ue6.selectorLabels" . | nindent 4 }}
